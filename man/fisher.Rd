% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_tests.R
\name{fisher}
\alias{fisher}
\title{Fisher's Method for Combining P-values}
\usage{
fisher(ps, pmin = 1e-06)
}
\arguments{
\item{ps}{A numeric vector of individual p-values to combine.}

\item{pmin}{Numeric. Minimum threshold for individual p-values to avoid taking \code{log(0)}. Defaults to \code{1e-6}.}
}
\value{
A numeric scalar representing the combined global p-value.
}
\description{
Combines multiple independent p-values into a single global p-value
using Fisher's combination test.
}
\details{
\itemize{
\item The test statistic is computed as \eqn{S = -2 \sum \log(p_i)}, where each \eqn{p_i} is thresholded by \code{pmin}.
\item The combined p-value is obtained by comparing \eqn{S} to a chi-squared distribution with \code{2 × length(ps)} degrees of freedom.
\item Useful for aggregating evidence across multiple tests while controlling type I error.
}
}
\examples{
\dontrun{
ps <- c(0.01, 0.05, 0.10)
fisher(ps)
}

}
\seealso{
\code{\link[stats]{pchisq}}
}
