% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validation.R
\name{cv.split}
\alias{cv.split}
\title{Generate Cross-Validation Splits with Approximately Equal Fold Sizes}
\usage{
cv.split(N, n.folds = 5)
}
\arguments{
\item{N}{Integer. Total number of samples to be split.}

\item{n.folds}{Integer. Number of cross-validation folds. Defaults to \code{5}.}
}
\value{
A list of length \code{n.folds}, where each element is an integer vector of indices for one fold.
}
\description{
Creates a list of indices that partition \code{1:N} into approximately equal-sized subsets for K-fold cross-validation.
The function ensures folds are randomly ordered and differ in size by at most one.
}
\details{
The function ensures:
\itemize{
\item Fold sizes differ by at most one.
\item Samples are randomly shuffled using \code{\link[base]{sample}}.
}
}
\examples{
set.seed(123)
folds <- cv.split(23, n.folds = 5)
sapply(folds, length)  # Check fold sizes (e.g., 5, 5, 5, 4, 4)
unlist(folds)          # Verify all indices 1:23 are included

}
\seealso{
\code{\link[base]{sample}} for random permutation.
}
