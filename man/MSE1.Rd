% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation_metrics.R
\name{MSE1}
\alias{MSE1}
\title{Mean Squared Error (MSE) Between Two Vectors}
\usage{
MSE1(est, truth, relative = FALSE)
}
\arguments{
\item{est}{A numeric vector of estimated values.}

\item{truth}{A numeric vector of true values, of the same length as \code{est}.}

\item{relative}{Logical. If \code{TRUE}, returns the relative MSE (normalized by \code{sum(truth^2)}). Defaults to \code{FALSE}.}
}
\value{
A numeric scalar representing the MSE or relative MSE.
}
\description{
Computes the mean squared error (MSE) between estimated and true values, with an option for relative MSE.
}
\details{
\itemize{
  \item If \code{relative = FALSE}, computes \eqn{\frac{1}{n} \sum_i (\hat{y}_i - y_i)^2}.
  \item If \code{relative = TRUE}, computes \eqn{\frac{\sum_i (\hat{y}_i - y_i)^2}{\sum_i y_i^2}}, which is scale-invariant.
  \item Missing values (\code{NA}) are ignored in the sum.
}
}
\examples{
set.seed(42)
truth <- rnorm(100)
est <- truth + rnorm(100, sd = 0.1)
MSE1(est, truth)
MSE1(est, truth, relative = TRUE)

}
