% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svd_helpers.R
\name{epca}
\alias{epca}
\title{Efficient Principal Component Analysis (EPCA)}
\usage{
epca(
  x,
  center = TRUE,
  scale = FALSE,
  retx = TRUE,
  k = min(nrow(x), ncol(x)),
  tol = 1e-06
)
}
\arguments{
\item{x}{A numeric matrix or data frame. Rows represent observations and columns represent variables.}

\item{center}{Logical. Should the variables be centered to have zero mean? Defaults to \code{TRUE}.}

\item{scale}{Logical. Should the variables be scaled to have unit standard deviation? Defaults to \code{FALSE}.}

\item{retx}{Logical. Should the principal component scores be returned? Defaults to \code{TRUE}.}

\item{k}{Integer. Number of principal components to compute. Defaults to \code{min(nrow(x), ncol(x))}.}

\item{tol}{Numeric. Eigenvalue tolerance. Components with eigenvalues below this threshold are discarded. Defaults to \code{1e-6}.}
}
\value{
A list containing:
\describe{
\item{\code{values}}{A numeric vector of eigenvalues scaled by \(1 / (n - 1)\).}
\item{\code{vectors}}{A matrix of principal component directions (eigenvectors). Columns are named \code{"PC1"}, \code{"PC2"}, etc.}
\item{\code{x}}{(If \code{retx = TRUE}) The principal component scores matrix.}
\item{\code{var.props}}{A numeric vector of cumulative variance proportions explained by the components.}
}
}
\description{
A fast and memory-efficient PCA routine compatible with and faster than \code{hd.eigen()} in the \pkg{Rfast} package.
Automatically handles wide and tall matrices. Returns scaled eigenvalues, principal components, scores (optional),
and cumulative variance proportions. The function is especially useful for high-dimensional data where standard PCA
methods are too slow or memory-intensive.
}
\details{
This implementation computes the SVD or eigendecomposition of the (possibly transposed) covariance matrix,
depending on the dimensionality of \code{x}. For wide matrices (\code{n < m}), it avoids computing the full covariance matrix
to save time and memory. Eigen decomposition is preferred for numerical stability and speed.
}
\examples{
\dontrun{
set.seed(123)
X <- matrix(rnorm(100 * 20), nrow = 100)
result <- epca(X)
plot(result$values, type = "b", main = "Eigenvalues")
}

}
\seealso{
\code{\link[Rfast]{hd.eigen}}, \code{\link[stats]{prcomp}}, \code{\link[stats]{eigen}}
}
